# Version output

Tests for snappy version commands.

## Version flag

```scrut
$ "${SNAPPY_BIN}" --version
snappy version v* (glob)
```

## Short version flag

```scrut
$ "${SNAPPY_BIN}" -v
snappy version v* (glob)
```

# Version output

Tests for snappy version commands.

## Version flag

```scrut
$ "${SNAPPY_BIN}" --version
snappy version * (glob)
```

## Short version flag

```scrut
$ "${SNAPPY_BIN}" -v
snappy version * (glob)
```
